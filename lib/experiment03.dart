import 'package:flutter/material.dart';

class Main extends Scaffold {
  const Main({super.key})
      : super(
          body: const Center(
            child: Text('Hello Me'),
          ),
        );
}
